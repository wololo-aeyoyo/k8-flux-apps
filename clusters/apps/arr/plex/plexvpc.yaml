apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: arr-stack
  labels:
    io.kompose.service: plex-claim0
  name: plex-claim0
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 30000Mi
