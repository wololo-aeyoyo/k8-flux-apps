apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-volume
  namespace: cnpg-system
spec:
  storageClassName: nfs-k8s-keep
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
---